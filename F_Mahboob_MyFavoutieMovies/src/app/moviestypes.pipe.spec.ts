import { MoviestypesPipe } from './moviestypes.pipe';

describe('MoviestypesPipe', () => {
  it('create an instance', () => {
    const pipe = new MoviestypesPipe();
    expect(pipe).toBeTruthy();
  });
});
